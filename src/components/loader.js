import React from 'react';
import '../css/loader.css'; // Import the CSS file for loader styling

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
